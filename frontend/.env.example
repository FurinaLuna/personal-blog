# 前端环境变量示例
#
# 开发环境默认走 vite.config.ts 里的 proxy，前端只发相对路径请求，
# 所以通常不需要配置任何变量。只有当 API 部署在别的域名上时才需要。

# 后端 API 基础路径。留空则使用默认值 /api/v1
VITE_API_BASE_URL=/api/v1
#
# 注意：站点标题**不在这里配** —— 它由后端站点档案（/api/v1/site/profile）
# 下发，改后台「站点设置」即可生效。曾经这里有一个 VITE_SITE_TITLE，
# 但源码里没有任何消费点，属于"文档承诺了、代码没实现"的死配置，已删除。
