# 统一换行符：仓库内一律 LF，避免 Windows 上出现 CRLF 与 LF 混杂
# （之前出现过 "LF will be replaced by CRLF" 警告，就是因为缺少这份约定）

* text=auto

# 源码与文本一律 LF
*.py        text eol=lf
*.ts        text eol=lf
*.mjs       text eol=lf
*.js        text eol=lf
*.vue       text eol=lf
*.css       text eol=lf
*.html      text eol=lf
*.md        text eol=lf
*.json      text eol=lf
*.toml      text eol=lf
*.ini       text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
*.mako      text eol=lf
*.example   text eol=lf
# shell 脚本一律 LF：带 CRLF 的脚本 COPY 进 Linux 容器后，执行时会以
# 「no such file or directory」失败（其实是行尾的 \r），报错方向完全是错的
*.sh        text eol=lf
*.svg       text eol=lf
Makefile    text eol=lf
.gitignore  text eol=lf
.gitattributes text eol=lf
.editorconfig text eol=lf
LICENSE     text eol=lf
Dockerfile* text eol=lf
# nginx 配置与它 include 的片段（deploy/*.conf、deploy/*.inc）一律 LF。
# 容器里跑的是 Linux nginx，而 Windows 检出默认可能是 CRLF：nginx 大多数情况下
# 能容忍 \r，但一旦它落进某个指令的**取值**里（例如多行 add_header 的安全头），
# 就会变成响应头里的一个真实字符 —— 症状只是"某个头看起来怪怪的"，极难归因，
# 与上面 *.sh 那条是同一类问题：行尾在容器里变成了语义。
*.conf      text eol=lf
*.inc       text eol=lf
.dockerignore text eol=lf

# Windows 专属脚本保持 CRLF
*.bat       text eol=crlf
*.cmd       text eol=crlf
*.ps1       text eol=crlf

# 二进制
*.png       binary
*.jpg       binary
*.webp      binary
*.ico       binary
*.db        binary
