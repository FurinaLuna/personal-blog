# 开发日志

本目录收录 personal-blog 项目的逐日开发日志，按 `YYYY-MM-DD.md` 命名。

日志内容：当天做了什么、关键决策（为什么这样做）、验证结果、踩坑备忘、
后续候选。与 `docs/DESIGN.md`（静态方案）互补——DESIGN 回答「系统长什么样」，
日志回答「它是怎么一步步变成这样的」。

## 写作约定

> **每一次改动都必须留下日志，并随改动一起提交推送。**
> 这条是硬约定，不是「有空再写」。

一篇日志至少覆盖四件事：

1. **改了什么**——文件与提交号，能对应到 `git log`；
2. **为什么这么改**——尤其是**放弃的方案**。只写结论的话，半年后看到
   这段代码的人（包括自己）会重新踩一遍已经否决过的路；
3. **怎么验证的**——跑了哪些命令、得到什么数字；实测出来的缺陷要写清
   复现方式（「CDP 连续进两篇文章并滚动」「两次 GET 对比时间戳」）；
4. **踩了什么坑 / 学到什么**——这一节最容易被省，也最值钱。
   凡是「排查了很久才发现」的，都值得写一行：下一个遇到的人可能就是你。

**尤其是 bug**：写清根因，而不是「修了个 bug」。日志里出现「把 bug 说成了
设计取舍」这种自我修正也照写——它比正确的结论更能防住下一次。

## 索引

| 日期 | 主要内容 |
|---|---|
| [2026-09-12](./2026-09-12.md) | 项目从零建成（FastAPI + Vue 3，145 测试全绿）；修复两个仅真实浏览器可见的 bug；创建 GitHub 仓库；四批迭代（代码块复制 + 移动端目录 / 主题三态 + 进度条 + hljs 按需注册 / RSS + sitemap + OG meta + 相关文章） |
| [2026-09-19](./2026-09-19.md) | 按分析报告开始落地：统一 XFF 信任策略（两层开关互相矛盾，实测伪造 5 个 XFF 得到 5 个访客）、`--workers 2` 的配额翻倍与冷启动竞态（含 SAVEPOINT 在 SQLite 上不隔离失败的实测）、Python 依赖加上界 + 提交完整锁文件 |
| [2026-09-17](./2026-09-17.md) | 全面审计并分七批落地 15 项改进（10 个提交）：安全三连（XFF 取值端 / 生产启动门禁 / 评论网址协议）、五处实测缺陷、前端健壮性与无障碍、工程工具链（import-linter 跨平台 / CI 接端到端 / ESLint / 备份）、SEO、定时发布、FTS5 中文全文检索、文章版本历史；以及搜索分页失效的二次修复 |
| [2026-09-22](./2026-09-22.md) | 三份独立审计后的落地（一轮一提交）：让 CI 真的能跑——抽出跨平台浏览器探测 `tools/lib/chrome.mjs`（`interaction-check` 此前写死 Windows 路径，导致 CI 的交互验证必失败、其后 41 项全功能回归从未执行）、`e2e_run.py` 改用 `sys.executable`、CI 新增 `e2e-live` 作业与 80% 覆盖率门槛、修正 pip 缓存键 |
