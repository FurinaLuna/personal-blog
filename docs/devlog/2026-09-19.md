# 2026-09-19 工作日志

> 承接 [2026-09-17](./2026-09-17.md) 的仓库分析报告，开始按计划落地。
> 当日工作分 Track A（正确性与安全）与 Track B（前端设计）两条线。

## 目录

- [批次 1 · 部署加固与依赖锁定](#批次-1--部署加固与依赖锁定)
- [批次 2 · CI 补漏与配置同步（A7）](#批次-2--ci-补漏与配置同步a7)
- [批次 2 附带发现并修复：一个已经推到 main 的严重回归](#批次-2-附带发现并修复一个已经推到-main-的严重回归)
- [批次 3 · 退订 token 移出 query string（A6）](#批次-3--退订-token-移出-query-stringa6)
- [批次 4 · Web 界面规范合规（B1）](#批次-4--web-界面规范合规b1)
- [批次 5 · 首页版式重做与圆角阶梯（B2 · frontend-design）](#批次-5--首页版式重做与圆角阶梯b2--frontend-design)
- [批次 6 · 令牌代次：让「改密码」真正能止损（A5）](#批次-6--令牌代次让改密码真正能止损a5)
- [批次 7 · 真的把 Docker 跑起来（A4）](#批次-7--真的把-docker-跑起来a4)
- [今日学习清单](#今日学习清单)
- [阻塞与待办](#阻塞与待办)

---

## 批次 1 · 部署加固与依赖锁定

A1 与 A2 改的是同一处（`deploy/Dockerfile.backend` 的 `CMD` 行与 compose 的
backend 服务），拆成两次提交会留下「文件改了一半」的中间状态，所以合并为一次
「部署加固」提交，日志里分开记述。

### A1 统一 XFF 信任策略

- **文件**：`deploy/Dockerfile.backend`、`docker-compose.yml`
- **根因**：项目里有**两个独立的 XFF 信任开关**，且默认配置互相矛盾 ——
  uvicorn 的 `--proxy-headers --forwarded-allow-ips *` 会在应用之前按
  `X-Forwarded-For` 改写 `scope["client"]`，于是应用层
  `TRUST_PROXY_HEADERS=false` 这道防线被完全架空：
  `client_ip()` 回落到 `request.client.host`，而那个值已经是客户端可控的。
- **实测确认**（本次会话，对本地后端）：

  | 启动方式 | 5 次不同伪造 XFF 后的 ΔUV |
  |---|---|
  | 默认（`--proxy-headers` 开，信任 127.0.0.1） | **5** —— 每个伪造值都是一个"新访客" |
  | `--no-proxy-headers` | **1** —— XFF 被忽略 |

  开发态就复现了，因为本机即 `127.0.0.1`，落在 uvicorn 的默认信任范围内。
  生产态更糟：`--forwarded-allow-ips *` 信任所有 peer，而 compose 又把
  `8000` 发布到宿主机，任何能直连后端的人都能伪造 IP，绕过全部限流。
- **修法**：把信任判定**收敛到应用层一处**。
  1. `Dockerfile.backend` 的 `CMD` 改为 `--no-proxy-headers`，uvicorn 不再插手；
  2. `docker-compose.yml` 给 backend 显式设 `TRUST_PROXY_HEADERS: "true"`
     （它确实在 nginx 之后，不开的话全网共用一个限流桶、UV 恒为 1）；
  3. **删掉 `ports: ["8000:8000"]`**，改为 `expose`。后端只应经 nginx 从
     compose 内网访问 —— 一旦映射到宿主机，nginx 那层的限流、CSP、
     安全响应头就全部失效。
- **验证**：`docker compose config` 静态校验通过，确认 `TRUST_PROXY_HEADERS: "true"`
  已注入、`ports` 已消失、只剩 `expose: ["8000"]`。

### A2 `--workers 2` 的两个后果

- **文件**：`deploy/Dockerfile.backend`、`backend/src/app/services/seed.py`
- **后果一：限流配额翻倍**。限流器是进程内的（`utils/ratelimit.py` 自己也
  写明了这个边界），而 `--workers 2` 让实际放行量乘以 2 —— 登录限流
  5/分 变 10/分。这不是理论风险，**镜像的默认配置就是它**。
- **后果二：冷启动竞态**。`seed_admin` 是 check-then-insert，两个 worker
  会双双通过 SELECT、双双 INSERT，后提交的撞 `users.username` 唯一约束；
  而 `lifespan` 里的异常会 **re-raise**，输掉的那个 worker 直接启动失败。

#### 修 seed 时踩的三个坑（都值得记）

**坑 1：教科书写法在本项目的主力数据库上不成立。**
第一版按 SQLAlchemy 的标准做法写了 `async with session.begin_nested(): await session.flush()`
再捕获 `IntegrityError`，想用 SAVEPOINT 把冲突隔离住。实测（SQLite 3.53 + aiosqlite）：

```
捕获到 IntegrityError: UNIQUE constraint failed: users.username
  对象是否还在 session 里: False
  是否可以继续查询：
  查询失败: PendingRollbackError
```

**SAVEPOINT 没有隔离这次失败** —— 整个 session 仍被置为「待回滚」，
后续任何查询都抛 `PendingRollbackError`。所以「捕获后回查一次就能救回来」
这个思路在这里根本走不通，不是代码写错，是前提不成立。

**坑 2：改用 upsert 后，`admin in session` 的 expunge 是多余的。**
在确认坑 1 之前我还试过「冲突后把对象摘掉再回查」，仍然失败 ——
因为 savepoint 回滚已经把对象摘掉了，而 autoflush 依然会重放那次插入。
这条弯路说明：**症状（PendingRollbackError）相同，根因可能完全不同**，
不在最小复现上确认就动手改，会连改两版都无效。

**坑 3：`from sqlalchemy import select` 的局部导入被顺手带出问题。**
改动过程中一次 `replace` 匹配到了错误的位置，留下了 35 行重复的残留
（旧的 `SiteProfile(...)` 参数块）。ruff 的 `invalid-syntax: Unexpected indentation`
直接指出来了 —— 大段替换后**立刻跑 lint** 是必要的，不能等到跑测试。

#### 最终修法

换成一条语句的原子 upsert：`INSERT ... ON CONFLICT DO NOTHING`。
SQLite ≥3.24 与 PostgreSQL ≥9.5 都支持，正好覆盖本项目支持的两个数据库。
把竞态从「查与插之间」压缩成一次原子写入，没有窗口可钻，
也就不需要异常处理路径 —— 顺带绕开了坑 1 和坑 2。

抽了 `_insert_ignoring_conflicts(dialect_name)` 做方言分发，
不支持的方言退回原来的 check-then-insert 并在注释里写明代价。

#### 「多 worker 现在安全了吗」——**没有，要分清**

修完 seed 后我用两个进程同时冷启动到同一个空库上实测：

```
:8032  HTTP 200       ← 正常启动
:8031  HTTP 000       ← 启动失败
  sqlite3.OperationalError: disk I/O error
users = 1             ← 没有重复，也没有唯一约束冲突
```

失败的进程拿到的是 **`disk I/O error` 而不是 `IntegrityError`** ——
说明 seed 的约束竞态确实修好了（没有重复 admin），但它死于另一个原因：
**两个进程并发对同一个 SQLite 文件做 DDL（`create_all`）本就不安全**。
这是 SQLite 的固有限制，不是 seed 的锅，也没有等价的 upsert 保护。

所以准确的说法是：**`--workers 1` 是当前架构下唯一正确的取值**，
要横向扩展得先换 PostgreSQL（解决并发 DDL）**并且**把限流换成共享存储
（解决配额翻倍）。这一条已写进 Dockerfile 的注释，避免下次有人"顺手调大 worker 数"。

- **验证**：
  - 单进程冷启动空库 → `users=1 profiles=1 articles=4`，`/health` 200
  - 重复启动 → 计数不变（幂等）
  - 两进程并发冷启动 → `users=1`，无唯一约束冲突
  - 新增 3 个用例（`tests/test_seed.py::TestSeedConcurrency`），
    用「只让第一次查重看不到已存在的行」精确模拟竞态窗口：
    断言走完「查不到 → 插入被忽略 → 回查」全过程，且**复用**而不是又插一条

### A3 Python 依赖锁定

- **文件**：`backend/pyproject.toml`、`backend/requirements.lock`（新增）、
  `.github/workflows/ci.yml`、`deploy/Dockerfile.backend`
- **根因**：全部依赖只有 `>=` 下界、无上界；唯一的锁文件 `uv.lock` 被
  `.gitignore:19` 排除。风险不是假设 —— **已经发生了**：

  | 包 | pyproject 写的 | 实际装出来的 |
  |---|---|---|
  | bcrypt | `>=4.2` | **5.0** |
  | aiosmtplib | `>=3.0` | **5.1** |
  | pillow | `>=11.3` | **12.3** |
  | pytest | `>=8.3` | **9.1** |

  都跨了一到两个主版本，只是恰好没坏。同一个提交在不同时间构建出的
  环境并不相同。

- **修法**（两步）：
  1. **加上界**：一律写成「下界 + 兼容上界」，允许当前已验证的主版本线、
     挡住下一个主版本。这是零维护的那一半，挡住的是"未知的未来主版本"。
  2. **生成完整锁**：在**全新的** venv 里装 `.[postgres,dev]`（用新 venv
     而不是复用开发环境，避免把调试包冻进去），再 `pip freeze`。
     锁里含 `asyncpg` —— 它只在生产镜像里用得上，而生产镜像正是靠这份锁构建的。
  3. CI 的 3 处安装步骤与 Dockerfile 都改成
     `pip install -r requirements.lock` + `pip install --no-deps -e .`
     （`--no-deps` 避免 pip 再解析一遍范围，与锁文件产生分歧）。

- **把「锁与范围必须一致」做成机器检查**：新增
  `tests/test_dependency_lock.py`（6 个用例）。漂移是必然会发生的 ——
  改了范围忘了重新生成锁，review 里几乎看不出来，症状却是
  「CI 装旧版本、本地装新版本，代码一个字没改」。用例覆盖：
  每个声明的依赖都在锁里、锁定版本满足声明范围、`asyncpg` 必须在
  （漏了它镜像构建直接失败，而那只有真跑 Docker 才会发现）。

  > 这个测试自己也抓到一个分类错误：第一版把 `build-system.requires`
  > （hatchling）也算作「必须进锁」，测试立刻报缺失。那是**测试的分类错了**
  > —— 构建后端由 pip 在隔离环境里临时安装，不进运行时环境。修的是测试。

- **验证**：`ruff` 全绿；`pytest` **379 passed**（新增 9 例）；
  `import-linter` 2 kept / 0 broken。

---

## 批次 2 · CI 补漏与配置同步（A7）

- **文件**：`.github/workflows/ci.yml`、`backend/.env.example`、
  `backend/tests/test_env_example.py`（新增）、`tools/full-check.mjs`
- **改了什么**：
  1. **e2e job 补跑 `full-check`**（41 项，写操作生命周期 + 数据基线核对）。
     此前 CI 只跑 smoke 与 interaction，而 full-check 恰恰是覆盖面最广的那个
     —— 这一点在本批被证明不是"锦上添花"，见下面 D8 那段。
  2. **新增 `audit` job**：`pip-audit -r requirements.lock` +
     `npm audit --omit=dev --audit-level=high`。单独成 job 而不是塞进
     backend job，是为了让失败时一眼能看出是"依赖有洞"而不是"代码挂了"。
     pip-audit 刻意**不**加进锁文件：安全扫描器需要最新漏洞库，
     钉死版本反而会漏公告。
  3. **`.env.example` 补全 5 个缺失字段**（`ALLOWED_IMAGE_TYPES` /
     `ALLOWED_FILE_TYPES` / `VISIT_LOG_RETENTION_DAYS` / `APP_NAME` /
     `API_V1_PREFIX`）。前两个是**安全相关**的——它们正是「默认禁 SVG」
     这条决定的落点，运维不知道能配，就既不会收紧也不会确认。
     并在两个白名单旁边写明了为什么默认不含 `image/svg+xml`。
  4. **新增 `tests/test_env_example.py`**：把「配置项必须文档化」做成机器检查，
     两个方向都查（漏文档 / 文档里有代码不认的变量）。后者会导致
     「我明明配了却不生效」——`extra="ignore"` 会静默丢弃。
  5. **`full-check` 的 D8 失败信息补全**：原文只输出 `回复提示=… 嵌套项=…`，
     漏掉了最关键的 `已发表`。这三者失败时看起来一样，排查方向却完全不同。

---

## 批次 2 附带发现并修复：一个已经推到 main 的严重回归

> **这一段是本日最值得读的部分。** 它说明：370 个后端用例 + 138 个前端用例 +
> smoke 34/34 + interaction 22/22 全绿，仍然可以漏掉「所有经编辑器发布的文章
> 对访客彻底不可见」。

### 现象

补跑 `full-check` 时 D8（二级评论）失败。第一版失败信息没说是哪一步失败，
补全后拿到 `已发表=false` —— 评论压根没出现。查后端日志：

```
POST /api/v1/comments/article/6 -> 404
```

文章 6 是 full-check 本轮新建并**已发布**的临时文章，评论却 404。

### 定位

404 来自 `comment_service` 的可见性检查。让 full-check 在后台跑、
**运行期间轮询数据库**，拿到文章 6 的真实状态：

```
id=6  status=published  published_at=None
```

**已发布，却没有发布时间。**

### 根因（是 2026-09-17 那批「定时发布」引入的）

`update()` 里我写的是：

```python
if "published_at" in data:            # ← 前端每次保存都会带这个字段
    requested = data.pop("published_at")   # None
    data["published_at"] = None            # ← 把 NULL 写回去
elif data.get("status") is PUBLISHED and article.published_at is None:
    data["published_at"] = datetime.now(UTC)   # ← 死代码，永远走不到
```

当时的**本意**是「显式传了时间就不能被『首次发布补当前时间』覆盖掉」，
所以用了 `elif`。但前端的 `buildPayload` 为了支持「清掉排期」，
**每次都显式传 `published_at: form.published_at || null`** ——
于是 `if` 永远命中，`elif` 成了死代码。

而「新建草稿 → 点发布」走的正是这条路径：草稿的 `form.published_at` 是空串
→ payload 里是 `null` → 发布后 `published_at` 仍是 `NULL`
→ `is_publicly_visible()` 返回 False → **对公众彻底消失**。

### 为什么这么难发现

三个"看不见"叠在一起：

1. **作者自己看得见**。`_can_view` 对作者放行，所以编辑器、后台列表、
   作者视角的详情页**一切正常**——写作者不会有任何察觉；
2. **列表页也可能看得见**（取决于查询路径），只有详情与评论是 404，
   现象零散；
3. **测试数据恰好绕开了它**。后端用例里创建文章时都是
   `status="published"` 且不传 `published_at` → 走的是**新建**路径
   （`_resolve_published_at` 正确补了 now）；而涉及改期的用例传的是
   **具体时间**，从没传过 `null`。也就是说：**测试发的请求和前端真正发的
   请求不是同一个形状**，这个缝隙正好是 bug 藏身之处。

`full-check` 是唯一走真实 UI 完整路径的，所以只有它抓到了。

### 修法

把不变量写清楚并直接编码：**published 状态不能没有发布时间**。

```python
status_after = data.get("status", article.status)
if data.get("published_at") is not None:
    data["published_at"] = _normalize_published_at(data["published_at"])  # 显式时间优先
else:
    data.pop("published_at", None)   # 没传或传 null：都不写 None
    if status_after is PUBLISHED and article.published_at is None:
        data["published_at"] = datetime.now(UTC)   # 缺了就补
```

关键差别：**不再用「字段在不在 payload 里」判断意图，而是用「值是不是 None」。**
前端总会带这个字段，所以「字段存在」不携带任何意图信息。

顺带抽出 `_normalize_published_at()`，让新建与更新共用同一套时区归一。

### 验证

```
修复前：草稿 -> PATCH {status:published, published_at:null}
        published_at=None   匿名详情 404   ← 对公众消失
修复后：published_at=<now>  匿名详情 200
对照：  已发布 -> PATCH {content_md, published_at:null}
        published_at 保持不变（重发一次不该让旧文掉出列表）
```

- `full-check` **41/41 通过**
- 新增 4 个用例（`TestPublishedAtInvariant`），其中一条直接断言
  「列表里出现的已发布文章都必须有时间」——即使将来实现再变，
  这条不变量也能兜住
- 存量数据核查：`published 且 published_at is null` 的文章 **0 篇**，
  用户真实数据未受影响（只有探针文章）

### 教训

1. **测试发的请求要和前端发的请求同一个形状。** 只要有一处不同（这里是
   「永远显式传 null」），那条缝隙就是 bug 的藏身处。E2E 走真实 UI，
   所以能盖住这类缝隙——这是它不可替代的价值。
2. **「用字段在不在来判断意图」是脆弱设计。** 前端为了支持「清空」总会带上
   字段，于是「在不在」不携带信息。判断依据应当是**值**。
3. **防御性的 `elif` 可能就是 bug 本身。** 我加它是为了「别覆盖显式时间」，
   结果它的前置条件（字段不存在）在实际调用下永假。
   写「保护性分支」时要问一句：**这个前置条件在真实调用下会成立吗？**
4. **作者可见 ≠ 公众可见。** 任何「只有作者能看到的失败」都必须在
   **匿名视角**下测一遍。这条以后要作为评审清单项。

---

## 批次 3 · 退订 token 移出 query string（A6）

- **文件**：`backend/src/app/services/notification_service.py`、
  `frontend/src/views/UnsubscribeView.vue`、
  `frontend/src/utils/unsubscribe.ts`（新增）、
  `backend/tests/test_notifications.py`、`backend/tests/test_wave3_verify.py`
- **根因**：退订链接生成的是 `/unsubscribe?token=<JWT>`，而 `deploy/nginx.conf`
  没有自定义 `log_format`，默认 `combined` 会记录完整 `$request`（含 query）。
  于是这个**有效期 10 年、凭它就能为任意邮箱退订**的凭证会落进访问日志，
  也会出现在 `Referer` 头里。
- **修法**：改成 `/unsubscribe#token=<JWT>`。fragment 由浏览器保留在本地、
  **不会随请求发给服务器**，因此不进 nginx 日志、不进 Referer、不进任何中间代理。
- **必须同时做的兼容**：改成 fragment 之前发出的邮件里是 `?token=xxx`，
  而那些链接有 10 年有效期 —— 只认新格式等于让已发出的邮件全部失效，
  用户点进去只会看到「链接不完整」。所以前端**两个来源都读，fragment 优先**。
- **顺带把取值逻辑抽成 `utils/unsubscribe.ts`**：它有正则、百分号解码、
  数组处理与回退，不是一行能覆盖的。抽出来配 9 个用例。

### 两个既有断言要跟着改（这是预期内的，不是回归）

- `test_notifications.py`：`assert "/unsubscribe?token=" in mail["text"]`
- `test_wave3_verify.py`：`re.search(r"/unsubscribe\?token=...")`

改它们的时候我在注释里指向了新增的 `TestUnsubscribeLinkFormat`，
免得下一个人以为是随手放宽断言。

### 验证

- `pytest` **388 passed**（新增 3 例）
- `vitest` **147 passed**（新增 9 例）
- CDP 实测两种形式都能退订成功：
  - `/unsubscribe#token=<有效>` → 页面显示「已退订」
  - `/unsubscribe?token=<有效>`（历史邮件）→ 同样成功
- 新增用例 `test_emitted_token_is_accepted_by_the_decoder` 做端到端校验：
  只断言「链接里有 `#token=`」是不够的 —— 格式对了但 token 本身有问题，
  用户点进去同样只会看到「链接无效」
- `smoke` 34/34；生产构建成功

---

## 批次 4 · Web 界面规范合规（B1）

按 `web-design-guidelines` 的规则逐条扫过前端。整体底子良好
（`focus-visible` 全局处理、无 `transition-all`、`prefers-reduced-motion` 已覆盖、
`aria-live` 已有、图标按钮无缺失 aria-label），真实缺口 6 类，全部修完。

### ⚠️ 先修正我自己审计里的一处误报

分析报告里我写的是「5 处 `<img>` 全部缺 `width`/`height`（CLS）」。逐一看容器后
发现**只有 1 处是真的**：

| 位置 | 实际保护 |
|---|---|
| `ArticleCard.vue:160` | img 自带 `aspect-video` + `sm:aspect-[10/7]` ✅ |
| `ArticleDetailView.vue:370` | 容器 `h-14 w-20` 固定尺寸 ✅ |
| `AboutView.vue:25` | `h-20 w-20` 固定尺寸 ✅ |
| `MediaView.vue:153` | 容器 `aspect-square` ✅ |
| `ArticleEditView.vue:556` | **无任何占位** ❌ |

规则的**目的**是防 CLS，而 CSS 已经预留了空间时再加 `width`/`height` 属性
既冗余又会被 `h-full w-full` 覆盖。所以只修了真正缺的那一处
（加 `aspect-video`，与列表卡片的封面比例一致——作者看到的就是读者看到的）。

**教训：审计结论要落到"这条规则要解决什么问题"上，而不是机械比对字面。**
按字面改会往 4 个已经正确的地方塞无用属性。

### 其余 5 项

| 项 | 落点 | 做法 |
|---|---|---|
| 跳到主内容 | `DefaultLayout.vue` | 全站第一个可聚焦元素，`sr-only` + `focus:not-sr-only` 浮出。**`<main>` 必须带 `tabindex="-1"`** —— 它默认不可聚焦，不加的话跳转后焦点不转移，键盘用户按 Tab 又从页面开头开始，「跳过导航」等于没跳 |
| `overscroll-behavior: contain` | `ConfirmDialog`、`MobileToc` | 防止在模态/抽屉里滚到底后继续滚动带动背后的页面 |
| `tabular-nums` | `DashboardView` 4 个 KPI、`TrendChart` 轴标签 | 比例数字里 1 比 8 窄，并排对比的数字右边界参差不齐。SVG 里用 `style` 而非 `class`（SVG 文本不支持 Tailwind 的 `font-variant-numeric` 工具类） |
| `touch-action: manipulation` | `base.css` | 消掉移动端 300ms 点击延迟。**只加在交互元素上，不加到 body** —— 正文里可能真有需要双击缩放的场景 |
| `focus-visible` 环 | `MarkdownEditor.vue:204` | 全站唯一一处 `outline-none` 没有替代 |

### 验证

- CDP 实测 skip link：第一次 Tab 聚焦到它且**可见**（`sr-only` 已在聚焦时解除），
  回车后焦点落到 `<main id="main">`、`location.hash === "#main"`
- `vue-tsc` 0 报错；`eslint` 0 问题；`vitest` 147 passed；生产构建成功
- `smoke` 34/34、`interaction` 22/22、`full-check` 41/41
  （skip link 是全站新增的第一个可聚焦元素，所以三个 E2E 都跑了一遍）

---

## 批次 5 · 首页版式重做与圆角阶梯（B2 · frontend-design）

### 设计计划（先写后做）

**子题**：中文个人博客，单作者，内容分技术 / 生活 / 读书三类；
读者多从搜索与 RSS **直达文章页**，首页面向回访与浏览。

**配色**：保留。纸白 `250 249 246` / 表面 `255 255 255` / 墨 `28 25 23` /
品牌蓝 `36 73 216` / 琥珀 `180 83 9`。

**字体**：保留。宋体系统栈做标题（零外链字体）、Inter + 中文栈做正文、
700px 行宽、1.75 行高。

**版面**：
```
改前（站名在同一屏出现两次）      改后（站名只由 sticky 顶栏承担）
┌──────────────────────┐        ┌──────────────────────┐
│ ■ 个人博客  首页 分类…│        │ ■ 个人博客  首页 分类…│
├──────────────────────┤        ├──────────────────────┤
│ 个人博客      ← 重复  │        │ 最新文章        共 4 篇│
│ 记录技术、生活…        │        │ ┌──────────────────┐ │
│ ──────────────────── │        │ │ 置顶 · 技术笔记    │ │
│ 最新文章      共 4 篇  │        │ │ 为什么我把博客的…  │ │
│ ┌──────────────────┐ │        │ │ （宋体 28px）      │ │
│ │ 卡片1（置顶徽标11px）│ │        │ │ 起因是老博客用了…  │ │
│ └──────────────────┘ │        │ │ 9月11日 · 8 分钟   │ │
└──────────────────────┘        │ └──────────────────┘ │
                                │ 更多文章              │
                                │ ┌──────────────────┐ │
                                │ │ 卡片2（18px 紧凑） │ │
```
左对齐；中文不用两端对齐。

**原则**
1. **不重复已经说过的话** —— 顶栏是 sticky 的、已用品牌标记 + 站名承担身份；
2. **结构装置必须编码信息** —— 分类、标签、时间是信息；`目录` 上的 `uppercase` 不是；
3. **圆角要有阶梯** —— 此前 6 档无规则。

### 自我批评（skill 要求先审视是否落入模板默认）

- ✅ **「头条」是常见博客版式，但它由作者已有的信号驱动**：列表默认「置顶优先」，
  所以头条就是作者亲自挑的那一篇。而在此之前，这个明确意图只被渲染成一个
  11px 的徽标 —— 作者说了「先看这篇」，界面没接住。
  它取代的「站名 + 标语 + 细线」恰恰是更模板化的那个（broadsheet 头部）。
- ⚠️ **规避 skill 点名的默认**：分类标签按中文原样渲染，**不加 `uppercase`
  也不加 `tracking`**（那正是被点名的 ALL-CAPS 眉标）；不加编号标记
  （首页文章不是序列，归档页才是）；不加渐变、不加装饰阴影、不加 `→`。
- ✅ **保留配色与字体**：`tokens.css:19-20` 有主题相关论证（中文阅读场景、
  冷暖对比），换掉是纯 churn 且丢掉已有论证。
- ❌ **不做全站重绘**：现有令牌系统有完整论证，测试不覆盖视觉，回归面过大。

### 改了什么

| 项 | 落点 |
|---|---|
| 首页 hero → 头条 | `HomeView.vue`：删掉重复站名的 `<header>`；`h1` 归「最新文章」；新增 `lead`/`rest` 计算属性；头条只在「无筛选的第一页」出现 |
| 头条待遇 | `ArticleCard.vue` 新增 `featured` 属性：标题转宋体 28px、摘要三行、封面 `sm:w-56`、内边距 `sm:p-8` |
| 分组标题 | 「**更多**文章」而不是「更早的文章」—— 排序可以切成最热/标题，那时它们并不「更早」 |
| 死样式 | 删掉 `TableOfContents` / `MobileToc` 的 `uppercase tracking-wide`：CSS `text-transform` **对汉字无效**，这是从拉丁排版照搬过来的 |
| 圆角阶梯 | 6 档 → 4 档并写进 `tokens.css`：`full` 圆形 / `md` 标记 / `lg` 控件 / `xl` 容器。`rounded-2xl`(5 处) 归入容器档，11px 徽标上的裸 `rounded`(7 处) 归入标记档。**原生 checkbox 的 4px 保留**——约 16px 的方块按 6px 会圆得过头 |

### 验证

- **CDP 实测**（脚本从 API 取真实站名，不硬编码）：首屏站名出现 **1 次**
  （修复前 2 次）；标语不在首屏但**仍在页脚**（定向信息没丢）；
  `h1=最新文章`、头条 `fontFamily=Noto Serif SC` / `28px` vs 普通卡片 `18px`、
  「更多文章」分组存在
- **样式基线比对**（项目自带的 `style-baseline` + `style-diff`）：
  27 处差异**全部落在首页**，且正是预期的那些（头条标题字体字号、
  分组标题、卡片内边距）；其余 7 个被采集路由零变化
  > ⚠️ 第一次采集的「before」是在**后端挂掉期间**拍的（132 vs 199 个元素、
  > 详情页 slug 取不到），那份对比是「无数据 vs 有数据」，无效。
  > 用 `git stash` 重拍后才得到可比的两份。**采集类工具必须先确认被测服务是活的。**
  > 另外要如实说明：基线只覆盖 8 个路由，而圆角改动多在 `/unsubscribe`、
  > `/links`、后台页（不在覆盖内），那些只有源码层面的验证。
- `vue-tsc` 0 报错；`eslint` 0 问题；`vitest` 147 passed；生产构建成功
- `smoke` 34/34、`interaction` 22/22、`full-check` 41/41

---

## 批次 6 · 令牌代次：让「改密码」真正能止损（A5）

- **文件**：`backend/src/app/models/user.py`、`utils/security.py`、
  `services/auth_service.py`、`api/deps.py`、
  `alembic/versions/20260919_1700_*.py`（新增）、
  `tests/test_token_version.py`（新增）

### 问题

refresh token 原本是**不可吊销**的：`refresh()` 只做「验签 + 查用户」，
签名有效且用户存在就签发新令牌。于是：

- 令牌泄露后，在 7 天有效期内**无法止损**；
- 「改密码」这个应急动作**只改了哈希** —— 攻击者手上那个还没过期的
  refresh token 照样能换出一对新的。等于**没有应急手段**。

这个问题 `docs/ASSESSMENT.md` 在 09-12 就记为 P1 了，一直没做。

### 做法：令牌代次（token versioning）

`users.token_version` 是一个整数代次：签发 JWT 时把它写进载荷（`ver`），
校验时与库里的当前值比对，不一致即视为失效。**改密码时 +1**，
该用户所有已签发的 access / refresh token 立刻作废。

用「代次」而不是「黑名单表」：一个整数就能表达「所有旧令牌作废」，
不需要为每个 `jti` 落库，也没有清理过期条目的问题。
（`jti` 本来就在载荷里，将来做单点登出时才需要它。）

四个接入点：`issue_tokens` 写入代次、`get_current_user` 与 `get_optional_user`
校验、`refresh` 校验、`change_password` 递增。

### 升级兼容（这一条最容易翻车）

本次改动**之前**签发的 token 里没有 `ver` 字段。如果解析时按「缺失即失效」
处理，一次发版就会把所有在线用户踢下线，而他们只会看到「登录已失效」。
所以 `decode_token` 对缺失的 `ver` **按 0 处理**，而所有存量用户的
`token_version` 默认就是 0 —— 老令牌继续有效。这条专门有用例锁住。

### 迁移里踩到的流程冲突（值得单独记）

`alembic upgrade head` 在开发库上**直接失败**：

```
sqlite3.OperationalError: table articles_fts already exists
```

根因是**两条 schema 路径会撞车**：开发默认 `DB_AUTO_CREATE=true`，
`create_all` 建了 `article_revisions`，应用启动钩子又建了 `articles_fts` ——
但 `alembic_version` 还停在 `c8d4e5f6a7b9`。此时 upgrade 会重放那两条迁移，
撞上「表已存在」。

其中「启动钩子建 FTS 表」是我在搜索那批**自己引入的**：为了让
「自动建表 + 不跑迁移」的开发路径也能搜索，我在 lifespan 里补建了虚拟表。
当时没想到它会让 alembic 迁移变得不可重放。

修法是让这两条迁移**幂等**（先 `inspect().has_table()` 判断）：应用和迁移
谁先建都可以，谁后跑就跳过。这比让使用者记住「先 stamp 再 upgrade」更可靠。

> 通用教训：**同一份 schema 有两个创建者时，两边都必须幂等。**
> 只让其中一边幂等，等于把顺序要求偷偷塞给了使用者。

### 验证

- `pytest` **396 passed**（新增 8 例），覆盖：改密码后 access 失效 /
  **refresh 失效** / 新密码可登录 / 旧令牌不会「复活」/
  **无 `ver` 的老令牌仍有效** / 代次默认是 0 而非 NULL / 两种令牌都能往返
- `alembic upgrade` → `downgrade -1` → 再 `upgrade` 双向验证；
  `alembic check` 中 token_version 相关漂移 **0**
  （第一版漏了模型侧的 `server_default`，被 `alembic check` 抓到 ——
  迁移里有、模型里没有，会让每次 `make migration` 都生成一条删默认值的噪音迁移）
- 真实服务端到端：改密码后旧 access 401、**旧 refresh 401**、
  新密码可正常登录、新 token 可用（跑完把密码改了回来）
- `smoke` 34/34、`interaction` 22/22、`full-check` 41/41
- 顺带清理了库里一个探针残留用户

---

## 批次 7 · 真的把 Docker 跑起来（A4）

- **文件**：`docker-compose.yml`、`.env.example`（新增，根目录）、
  `deploy/Dockerfile.backend`、`deploy/Dockerfile.frontend`、
  `backend/scripts/docker-entrypoint.sh`（新增）、`.gitattributes`、
  `Makefile`、`backend/src/app/main.py`、
  `backend/tests/test_deploy_config.py`（新增）、
  `tools/interaction-check.mjs`、`README.md`、`docs/DESIGN.md`

### 这一批的意义：Docker 从来没被真跑过

A4 在计划里只是"实跑一次验证"，结果一跑就露出**三个一跑就炸的问题**。
它们的共同点是：**静态读代码看不出来，只有真跑一次才暴露**。

**① 前端镜像根本构建不出来。**
`deploy/Dockerfile.frontend` 里写着 `COPY deploy/nginx.conf`，但这个镜像的
构建上下文是 `frontend/` —— 上下文之外的文件 COPY 不到，构建直接失败：

```
failed to compute cache key: "/deploy/nginx.conf" not found
```

**② compose 的配置源是错的。**
文件头部让人 `cp backend/.env.example backend/.env`，可 compose 的 `${VAR}`
插值**只读项目根目录的 `.env`**、不读服务的 `env_file`。照着文档配完，
`docker compose up` 报 `required variable POSTGRES_PASSWORD is missing`。
文档和它描述的工具对不上，而两边单看都很合理。

**③ 部署默认值是开发值。**
compose 读的那份配置里是 `APP_ENV=development`、`DEBUG=true`、
`DB_AUTO_CREATE=true` —— 也就是说 compose 上的"生产部署"实际以开发模式
对外服务：`/docs` 敞开、异常带堆栈、改错模型会在生产库上静默改表。
生产启动门禁也拦不住，因为它只在 `APP_ENV=production` 时才生效。

> 通用教训：**「文档写的」和「工具读的」必须是同一处，否则两边都对、合起来错。**
> 这种错还特别难发现——因为每一步看起来都在按文档做。

### 做法

**配置单一来源**：根目录 `.env.example` 是 Docker 部署唯一要读的文档，
compose 的 `env_file` 也指向同一个根 `.env`（与插值同源，杜绝
"改了 .env 但容器里还是旧值"的分裂状态）。

**密钥缺失就 fail fast**：`${POSTGRES_PASSWORD:?…}`、`${JWT_SECRET_KEY:?…}`、
`${ADMIN_PASSWORD:?…}` 让 `docker compose up` 在**插值阶段**就报错。
应用侧本来就有 `check_production_safety()` 兜底，但那要等容器起来才报，
表现出来是 restart 循环 + 得翻容器日志；compose 这层更早、更直白。

**部署默认落在生产侧**：`APP_ENV=production`、`DEBUG=false`、
`DB_AUTO_CREATE=false`、`SEED_DEMO_DATA=false` 由 compose 写死默认值，
想在本机试玩就在根 `.env` 里显式覆盖。

**迁移交给容器入口**（`backend/scripts/docker-entrypoint.sh`）。
这一条是被上一批的改动逼出来的：`DB_AUTO_CREATE=false` 之后，
"忘了跑迁移"的后果是应用起不来，而原文档让人在 `up -d` 之后 `exec` 进去补迁移 ——
那时候容器正在 restart 循环里，`exec` 根本进不去（**文档给的补救步骤本身不可执行**）。
入口脚本先 `alembic upgrade head` 再 `exec "$@"`，`docker compose up -d --build`
才真的是一条命令可用。

**`additional_contexts`**：nginx.conf 留在 `deploy/`，由 compose 额外挂一个
构建上下文进来，而不是往 `frontend/` 复制一份配置 —— 两份配置迟早漂移，
而"改了没生效"在 nginx 上排查代价很高。

**顺手做的两处构建提速**（都是被这次的构建耗时逼出来的）：
- 去掉 builder 阶段的 `build-essential`。锁文件里 51 个包全都提供 cp312 的
  manylinux wheel，实测整条 `pip install` 输出里**一条 `Building wheel` 都没有**，
  编译器一次都没被调用，而它要从 deb.debian.org 拉约 200MB、首次构建实测多花
  3 分半。去掉后重新构建通过 —— 证明这个判断成立。
- 把 `COPY src` 挪到依赖安装**之后**。原先是 `COPY src` 和
  `pip install -r requirements.lock` 挤在同一个 RUN 里，于是**改一行业务代码
  就要重装一遍全部依赖**。拆开后同样的代码改动重建只花 **20 秒**（依赖层命中缓存）。

### 踩的坑

**镜像源全挂了。** 构建第一步就失败：

```
dialing docker.mirrors.ustc.edu.cn:443: no such host
```

USTC 的 Docker 镜像站已经停服，163 的那个也早停了，两个都写在
Docker Desktop 的 `registry-mirrors` 里；而直连 `registry-1.docker.io` 不通。
换用还活着的镜像源、以 `docker pull <镜像源>/library/xxx` 显式拉取再本地
`docker tag`，**不动 Docker Desktop 的配置**就绕过去了（也就不必重启守护进程、
不会影响你机器上其他正在跑的容器）。

**CRLF 会让 shell 脚本以"文件不存在"的形式失败。** 带 `
` 的 `.sh` 进容器后
报的是 `no such file or directory`，方向完全是错的。`.gitattributes` 里
原先只钉了 `*.py/*.ts/*.md/...`，**没有 `*.sh`** —— 而 Windows 检出的工作区
就是 CRLF。所以既补了 `*.sh text eol=lf`，Dockerfile 里也留了一道
`sed -i 's/
$//'` 兜底。

**`docker compose run` 也会走 ENTRYPOINT。** 用它验证生产门禁时，入口脚本
先把迁移跑了一遍才轮到 uvicorn 报错 —— 这是预期行为，但值得记一下，
否则会以为"迁移怎么又跑了一次"。

### 这次没有靠"下次注意"，而是钉成了用例

`backend/tests/test_deploy_config.py`（新增 10 例）把上面每一条都变成断言：

- **COPY 的源必须落在构建上下文里**（解析 compose 的 `build.context` +
  Dockerfile 的 COPY/ADD）——就是①那条；
- **`COPY --from=X` 的 X 必须是已声明的构建阶段或 additional_contexts**；
- **compose 插值读的变量必须写在根 `.env.example` 里**，反向也查
  （写了但 compose / 入口脚本 / `config.py` 都不读的变量，是拼错或已删除，
  两种都表现为"我明明配了却不生效"）；
- **三个密钥必须用 `:?` 校验**；
- **容器默认值必须落在安全侧**（`APP_ENV=production` 等）；
- **入口脚本不得含 CRLF**，且 `.gitattributes` 里钉了 `*.sh eol=lf`。

写完顺手做了变异验证：把 `COPY --from=deploy nginx.conf` 改回原来的写法、
把 `env_file` 改回 `backend/.env`，两条用例分别失败、正好命中 ——
**用例确实抓得住原来的 bug，不是摆设。**

### 验证（全部在真实容器拓扑里）

- 三个容器全部 healthy；`docker compose build` 两个镜像都构建成功
- 入口脚本在**空的 PostgreSQL 上从零跑完 8 条迁移**（含上一批改成幂等的
  FTS5 / article_revisions 两条），随后启动日志是 `env=production` ——
  说明生产门禁在真实配置下是**通过**的
- PID 1 就是 uvicorn（`exec` 生效），启动参数 `--workers 1 --no-proxy-headers`，
  进程 UID=1000（非 root）
- 经 nginx 的 `/health`、`/api/v1/site/profile`、SPA 深链（`/archive`）全部 200；
  容器内直连 `/docs` 是 404（生产模式按预期关闭）
- 用 `.env` 里的口令登录成功、错误口令 401；建了一篇 `published` 文章，
  **`published_at` 非空**且**匿名访客能读到 200**（D8 那个严重回归在 Postgres 上的复核）
- **A1 复验（伪造请求头）**：同一篇文章，伪造 5 个不同 `X-Forwarded-For`
  访问 → 库里 6 条访问记录、**去重访客数 = 1**；再伪造 5 个不同
  `X-Real-IP` → 累计 11 条记录、**去重访客数仍是 1**。
  第二组是关键：它证明 nginx 用的是 `proxy_set_header`（覆盖）而不是追加 ——
  「X-Real-IP 优先」这条安全论证正是建立在这一点上
- **生产门禁实测**：用仓库里的公开默认密钥起容器，进程以退出码 1 拒绝启动，
  并逐条列出问题（这才是"门禁"该有的样子，而不是打条警告继续跑）
- 浏览器 E2E **直接打容器里的构建产物**：`smoke 34/34`、
  `interaction 22/22`（`interaction-check` 原先硬编码 5173，加了
  `INTERACTION_BASE` 环境变量才能指向 8080；没有改它已有的 argv 语义）
- 常规检查：`pytest` **406 passed**（新增 10 例）、ruff / format /
  import-linter / eslint / vue-tsc / vitest(147) 全过

---

## 今日学习清单

1. **同一件事有两层开关时，默认配置很可能互相矛盾。**
   `TRUST_PROXY_HEADERS=false` 看起来是安全的默认值，但只要 uvicorn 的
   `--proxy-headers` 开着，这个开关就永远读不到真实值。
   查安全开关时不能只看自己写的那一层。
2. **「实测」要选对观测口径。** 一开始我看的是 uvicorn 的 access log，
   它显示 `127.0.0.1` —— 但那不反映改写后的 scope。换成**访客 UV**
   才看到真相（ΔUV=5）。口径选错会得出"没问题"的相反结论。
3. **SAVEPOINT 在 SQLite/aiosqlite 上不隔离 flush 失败**（见坑 1）。
   教科书方案要先用最小复现验证前提，再动手改业务代码。
4. **症状相同不代表根因相同**（见坑 2）。`PendingRollbackError` 出现了两次，
   根因却是两个不同的东西。
5. **大段替换后立刻跑 lint**（见坑 3）。35 行重复残留是 ruff 先发现的，
   不是测试。
6. **修完一个连环故障要分清哪一段真的修好了。** 两进程并发冷启动仍然
   失败，但失败原因从 `IntegrityError` 变成了 `disk I/O error` ——
   这是"修好了甲、暴露了乙"，不是"没修好"。日志里必须写清楚，
   否则下一个人会以为修复无效。
7. **约定要变成测试才有约束力。** 「改了 pyproject 要重新生成锁」写在注释里
   等于没写；`test_dependency_lock.py` 才是那个会拦住你的东西。
8. **测试发的请求要和前端发的请求同一个形状**（见 D8）。只要有一处不同，
   那条缝隙就是 bug 的藏身处——后端用例永远传具体时间，前端永远传 null，
   于是「首次发布草稿」这条路径在所有单测里都是空的。
9. **「用字段在不在来判断意图」是脆弱设计**（见 D8）。调用方为了支持"清空"
   总会带上字段，此时「在不在」不携带任何信息，判断依据必须是**值**。
10. **写保护性分支时要问：这个前置条件在真实调用下会成立吗？**（见 D8）
    我加 `elif` 是为了"别覆盖显式时间"，而它的前置条件（字段不存在）永不成立，
    于是保护分支本身成了 bug。
11. **作者可见 ≠ 公众可见。** 任何"只有作者能看到的失败"都要在**匿名视角**
    下再测一遍。这条应当进评审清单。
12. **采集类工具必须先确认被测服务是活的**（见 B2）。style-baseline 的
    「before」在后端挂掉期间拍的，两份基线比的是"无数据 vs 有数据"，
    27 处差异里绝大多数是内容差异而非样式差异。**在错误的前提上做比对，
    得到的不是噪声而是一个看起来煞有介事的错误结论。**
13. **重复的信息会稀释版面**（见 B2）。站名在同一屏出现两次不是"强化品牌"，
    而是让第二处不再携带信息、白白占掉第一屏。判断一处内容该不该在，
    问的是「它在这里提供了别处没有的信息吗」。
14. **同一份 schema 有两个创建者时，两边都必须幂等**（见 A5）。
    只让其中一边幂等，等于把「先跑哪个」的顺序要求偷偷塞给了使用者 ——
    而使用者根本不知道有这么个要求，只会看到一个「表已存在」的报错。
15. **交付物没被真跑过，就等于没有交付物**（见 A4）。Docker 相关的三个问题
    全都是"照着文档一步步做、每一步看起来都对、合起来直接报错"。
    文档与工具的一致性，只能靠真跑一次来证明。
16. **文档给的补救步骤本身可能是不可执行的**（见 A4）。"`up -d` 之后再 exec
    进去补迁移"—— 而那时容器正在重启循环里，`exec` 进不去。
    写补救步骤时要在**故障状态下**走一遍，而不是在正常状态下想象。
17. **审计结论要落到"这条规则要解决什么问题"上**（见 B1）。「img 缺宽高」
    的字面检查报了 5 处，逐一看容器后只有 1 处真的会 CLS ——
    CSS 已经预留空间时再加属性既冗余又会被覆盖。按字面改会往 4 个已经正确的
    地方塞无用属性。

---

## 阻塞与待办

- **A4 已完成**（见批次 7）。遗留一件**环境**问题（不在仓库里）：
  Docker Desktop 的 `registry-mirrors` 里两个源（`docker.mirrors.ustc.edu.cn`、
  `hub-mirror.c.163.com`）都已停服，而直连 `registry-1.docker.io` 不通。
  本次是靠显式指定可用镜像源 + 本地 `docker tag` 绕过去的（没有动你的
  Docker Desktop 配置）。长期建议把 `~/.docker/daemon.json` 里的
  `registry-mirrors` 换成还活着的源再重启 Docker Desktop ——
  否则任何一次 `docker pull` 新镜像都会卡在解析失败上。
- 计划内还剩：**B3 及之后的设计/体验项**（按 `docs/ROADMAP.md` 推进）。
- 本批新增的部署约定（根 `.env`、生产侧默认值、入口自动迁移）已写进
  `README.md` 与 `docs/DESIGN.md` 第 5 章；`Makefile` 加了 `docker-config`，
  缺变量或写错上下文时不用等到构建失败才发现。


- **A4（实跑 Docker 验证）被阻塞**：Docker CLI 已安装（29.7.2 / compose v5.4.0），
  但 **Docker Desktop 守护进程未运行**：
  ```
  failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine
  ```
  本批只能用 `docker compose config` 做静态校验。**需要你启动 Docker Desktop**，
  之后我会跑 `docker compose up -d --build` + 迁移，并复验 A1/A2 在真实容器
  拓扑下的行为（尤其是 `--no-proxy-headers` 与 nginx 的配合）。
- 顺带发现、本批未处理：`docker compose config` 显示 compose 读的是
  `backend/.env`，而它当前是**开发配置**（`APP_ENV=development`、`DEBUG=true`、
  默认 JWT 密钥）。也就是说直接 `docker compose up` 会以开发模式跑生产，
  生产启动门禁（`check_production_safety`）**不会触发**（它只在
  `APP_ENV=production` 时生效）。待确认是否要加一道保护。
- 后续批次：A7（CI 补 full-check / pip-audit、`.env.example` 补 5 字段）、
  A5（`users.token_version`）、A6（退订 token 改 fragment）、
  B1（web-design-guidelines 合规修复）、B2（frontend-design 设计打磨）。
