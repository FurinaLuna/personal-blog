# 2026-09-12 工作日志

> 本次会话自 2026-09-11 21:48 开始，跨过零点，主要产出集中在这一窗口。

## [00:05] - 功能实现：新建个人博客全栈项目（FastAPI + Vue 3）

- **产出**: `D:\Projects\01_个人项目\personal-blog\`（新建，约 100 个源文件）
- **交付内容**:
  - 后端：FastAPI + SQLAlchemy 2.0 全异步 + Pydantic v2 + JWT 双 Token，
    分层为 `api / services / repositories / models / schemas / db / utils`
  - 前端：Vue 3 + TS + Vite + Pinia + Tailwind，9 个前台页 + 8 个后台页 + 3 套布局
  - 数据模型：users / categories / tags / articles / article_tags / attachments / comments / site_profile
  - 部署：Dockerfile×2 + nginx.conf + docker-compose + Makefile + Alembic 迁移
  - 文档：`docs/DESIGN.md`（五大模块完整方案）+ `README.md` + 7 张界面截图
- **决策**:
  1. 正文存 Markdown 原文，渲染放前端（marked + DOMPurify 白名单消毒）
  2. 自定义 `UTCDateTime` 类型：全站时间口径统一为「UTC 带时区」
  3. 时间戳用 Python 侧 default/onupdate，避免数据库侧 onupdate 导致的异步属性过期
  4. 上传默认禁 SVG/HTML（存储型 XSS 面），文件名由服务端 UUID 生成
  5. 前端筛选/排序/分页状态全部放 URL query
  6. JWT 放 localStorage，权衡与升级路径已写入文档 4.7 节
- **验证**:
  - `ruff check` 通过；`ruff format --check` 65 文件已格式化
  - `pytest` **145 passed**
  - `vue-tsc --noEmit` 零报错；`vite build` 成功（gzip 主包 ~29KB，总计 637KB）
  - `alembic upgrade head` 建 8 张表；`downgrade base` 干净回滚
  - `tools/smoke-check.mjs` 真实 Chrome 端到端冒烟 **34/34 通过**，页面错误 0

## [00:10] - Bug 修复：两个只有真实浏览器才能发现的前端问题

- **文件**: `frontend/src/stores/auth.ts`、`frontend/src/main.ts`、`frontend/src/router/index.ts`
- **Bug 1**：登录后访问后台被弹回登录页。根因是 `restore()` 里
  `if (restoring.value) return` 让并发调用者不等待进行中的恢复，守卫拿到 `user=null`
  就判定未登录（而 `/auth/me` 其实已返回 200）。修复：并发调用方共享同一个 Promise；
  并在 `main.ts` 改为 `router.isReady().then(mount)`，消除「挂载早于守卫」的窗口。
- **Bug 2**：后台侧边栏渲染两遍、子页面完全不显示（列表接口根本没发）。
  根因是 `/admin` 父路由既挂了 `component: AdminLayout` 又声明了 `meta.layout`，
  导致布局被嵌套两层，内层拿不到 slot 内容。修复：父路由不写 component，布局只走 meta。
- **验证**: 冒烟从 25/34 → **34/34**；`vue-tsc` 与 `vite build` 仍全绿

## 待办 / 后续可做

- 未做：RSS / sitemap、全文检索（当前是 LIKE）、图片处理走对象存储、评论反垃圾
- 生产上线前需按 `docs/DESIGN.md` 5.7 的 PRR 清单逐项过，尤其：
  换 `JWT_SECRET_KEY`、`DB_AUTO_CREATE=false`、`SEED_DEMO_DATA=false`、改默认管理员密码

## [00:20] - 规划输出：优化美化与功能升级路线图（仅建议，未改码）

- **产出**: 三档优先级路线图（已用可视化交付），基于实际代码给出落点
- **快速见效（1-2 天）**: 代码块复制按钮（`utils/markdown.ts` 渲染后补按钮）、
  阅读进度条、移动端浮动目录（当前 TOC 仅 `xl` 断点显示）、主题三态（亮/暗/跟随系统）
- **中等投入（3-7 天）**: RSS + sitemap + OG 分享卡片（当前 `index.html` 只有静态 description）、
  相关文章推荐、图片转 AVIF
- **长期（1 周+）**: 全文检索、草稿自动保存 + 版本历史、对象存储、前端测试（Vitest）
- **已识别的具体性能点**: `vite.config.ts` 的 markdown chunk 233KB（gzip 77KB）偏大，
  因为 `highlight.js/lib/common` 打包了约 40 种语言；改为按需注册 8-10 种可降到 ~60KB
- **判断**: 若要解决 SPA 的 SEO 硬伤，最彻底的是把前台换成 Astro/Nuxt（SSR），
  但这属于重写而非优化，成本需单独评估

## [00:36] - 版本管理：创建 GitHub 仓库并完成首次推送

- **仓库**: https://github.com/FurinaLuna/personal-blog （Public，154 文件 / 19673 行）
- **提交**: `b9980f6` feat: 个人博客全栈项目（FastAPI + Vue 3）
- **topics**: blog / fastapi / vue3 / typescript / sqlalchemy / tailwindcss / fullstack / jwt / docker
- **关键过程**:
  1. 本地 `git init -b main` + 补 `.gitattributes`（统一 LF，此前有 CRLF 警告）+ 首次提交
  2. `gh` CLI 未安装；GitHub 连接器未挂载 MCP 工具；但 GCM 里存有可用 PAT（scopes 含 repo）
  3. 用 `git credential fill` 取 token（**全程未打印 token**）→ 调 `POST /user/repos` 建库
  4. HTTPS 推送报 `schannel: server closed abruptly` → 改走 SSH（`~/.ssh/config` 已配置
     `ssh.github.com:443`）→ 推送成功
- **踩坑备忘**:
  - GitHub **不支持 push-to-create**（GitLab/Gitea 支持），必须先建库再推
  - 本机 schannel 对 github.com HTTPS 推送不稳定，**git 走 SSH 走 443 是可靠路径**
  - `git credential fill` 可无交互取出 GCM 存的凭据，适合脚本化调用

## [00:51] - 功能实现：代码块复制按钮 + 移动端浮动目录（快速见效档前两项）

- **文件**: `utils/markdown.ts`、`MarkdownRenderer.vue`、`MobileToc.vue`(新)、
  `ArticleDetailView.vue`、`style.css`（5 文件，+300/-3）
- **设计**: 结构归渲染流水线（.code-block / .code-lang / .code-copy 随 v-html 生成），
  行为归组件（事件委托，几十个代码块只挂一个监听器）；
  复制按钮在右上、语言标签在右下，分居上下天然不重叠
- **降级路径**: clipboard API 不可用（非 HTTPS / 无权限）→ execCommand → 仍失败则 toast
- **验证**:
  - vue-tsc 0 报错；vite build 通过；smoke **34/34** 无回归
  - 复制反馈实测（stub 剪贴板）：点击后「已复制」+ data-state=done，1.4s 还原，
    实际写入 166 字符
  - 移动端抽屉实测：FAB 出现、6 个目录项、点击可展开
- **已提交推送**: `9ed8269` → origin/main

## [00:52] - 下一批候选（未实现）

- 阅读进度条（可用 CSS animation-timeline，优雅降级）
- highlight.js 按需注册语言（markdown chunk 233KB → 约 60KB）
- 正文行高 1.85 → 1.75、中英文间距、封面图 aspect-ratio 占位
- 主题三态（亮/暗/跟随系统）

## [01:17] - 第三批迭代：主题三态 + 阅读进度条 + hljs 按需注册（c2bc4b6，已推送）

- **主题三态**：`stores/theme.ts` 重构为 light/dark/system（resolved 分离），
  system 态实时跟随 prefers-color-scheme；ThemeToggle 三图标循环；
  index.html 首屏脚本兼容 'system' 值
- **阅读进度条** `ReadingProgress.vue`：详情页顶部 2px 条，JS rAF 节流 +
  passive listener（不用 CSS scroll-timeline —— Safari 26 才支持）
- **hljs 按需注册**：`lib/common`（~40 语言）→ `lib/core` + 17 种，
  markdown chunk 233KB → **90KB**（gzip 77KB → 31KB）
- **行高**：tailwind.config.js 中文正文 1.85 → 1.75
- **验证**：vue-tsc 0 报错；vite build 通过；smoke **34/34**；
  专项验证（进度条挂载/滚动更新/高亮仍生效/主题循环）**4/4**
- **踩坑备忘**:
  - 详情页路由是 `/article/:slug`（单数），后台才是 `/admin/articles`；
    测试脚本写成复数会撞 NotFoundView
  - Vite 改 import 子路径（如 highlight.js/lib/core）触发依赖重新优化 →
    首次冒烟拿到白屏，重跑即恢复；判断依据：页面错误为 0 且整页空白
  - 主题按钮「跟随系统」态的 label 不含「模式」二字，测试按「模式」找按钮会漏

## 下一批候选

- 相关文章推荐、图片转 AVIF、封面图 aspect-ratio 占位
- 草稿自动保存 + 版本历史、全文检索、对象存储、前端测试（Vitest）

## [02:05] - 第四批迭代：SEO 三件套 + 相关文章（f0e6e47，已推送）

- **后端**: `/feed.xml`（RSS 2.0，20 篇，绝对链接）、`/sitemap.xml`、
  `GET /articles/{id}/related`（同分类或共享标签，排除自身/草稿）；
  config 新增 `site_base_url`；+9 pytest（共 **154**）
- **前端**: `useHead` composable（标题 + og/twitter meta，路由标题为底业务标题为盖）、
  详情页「相关阅读」、8 页接入 useHead、封面 aspect 占位、页脚 RSS + autodiscovery
- **部署**: vite 代理 + nginx 增加 `/feed.xml`、`/sitemap.xml` 路由
- **验证**: ruff 通过、154 passed、vue-tsc 0、build 通过、smoke **34/34**、专项 **7/7**
- **踩坑备忘**:
  - `useHead` 初版卸载时把标题重置为站点名，会把路由 afterEach 写的
    「X · 个人博客」冲掉（旧组件卸载晚于新路由 afterEach）——解法：以
    `route.meta.title` 为回退底，卸载时自然交还
  - 冒烟脚本断言 `document.title.includes('首页')` 正是这个约定兜住的，改动
    标题机制时先查冒烟断言
